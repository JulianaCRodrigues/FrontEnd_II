 export function multiplicacao(num1, num2) {
  if (num1 === 0 && num2 === 0) {
    return 0;
  }
  return num1 * num2;
}

//export  comando para funcar ser exportada para outro local

//console.log(multiplicacao(2,2))
